# Restriction - SQL

En SQL, pour réaliser une opération de restriction, on utilise simplement
la clause `WHERE`.

## Syntaxe

```sql
SELECT *
	FROM table
	WHERE condition
```

## Exemple sur les données